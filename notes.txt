dependencies vs dev dependencies


Will Babel transpile to a single file from a folder? NOPE


Refactors:
- App handles hiding and showing all form pages
  - Single 'move around' function that takes parameters

Priorities:
- Submit form data to something (log it for now)